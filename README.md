Note: The application uses MongoDB Atlas free tier.
The database may auto‑pause during inactivity and resume automatically on first request.
It may take 2-3 minute on the first request and should run smoothly after the first render

